/**
 * App Guards — Placeholder
 *
 * Location reserved for:
 *   - AuthGuard     (redirect to /login if unauthenticated)
 *   - RoleGuard     (render 403 if insufficient role)
 *   - TenantGuard   (ensure active tenant is resolved)
 *
 * Will be implemented in the authentication + RBAC phase.
 */

export {};
