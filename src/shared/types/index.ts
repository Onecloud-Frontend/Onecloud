/**
 * OneCloud Frontend — TypeScript Type Foundations
 *
 * This directory will contain:
 *   - api.types.ts     → ApiResponse<T>, ErrorResponse, PageResponse<T>
 *   - auth.types.ts    → AuthResponse, LoginRequest, RegisterRequest
 *   - common.types.ts  → BaseEntity, pagination params, sort params
 *
 * Domain-specific types (HRMS, CRM, etc.) will be added ONLY after
 * final backend service contracts are defined and agreed.
 *
 * Current state: placeholder reserving the correct architecture location.
 */

export {};
