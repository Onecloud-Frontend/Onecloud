/**
 * Core Tenant Infrastructure — Placeholder
 *
 * Location reserved for:
 *   - TenantContext (active tenantId, setTenant)
 *   - useTenant hook
 *   - tenantStorage (persist selected tenant)
 *   - Tenant-aware API header management (X-Tenant-ID)
 *
 * Reference: backend TenantFilter.java reads X-Tenant-ID header.
 * Default tenant ID: "default" (from application.yml)
 *
 * Will be implemented in the multi-tenancy phase.
 */

export {};
