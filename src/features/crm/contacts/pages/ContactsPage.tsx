import React from 'react';
import PageShell from '@/shared/components/ui/PageShell';

/** Route: /crm/contacts */
const ContactsPage: React.FC = () => (
  <PageShell domain="CRM" title="Contact Management" description="Customer and prospect contact database, communication history, and segmentation." />
);
export default ContactsPage;
