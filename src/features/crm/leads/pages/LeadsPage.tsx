import React from 'react';
import PageShell from '@/shared/components/ui/PageShell';

/** Route: /crm/leads */
const LeadsPage: React.FC = () => (
  <PageShell domain="CRM" title="Lead Management" description="Lead capture, qualification, scoring, and assignment workflows." />
);
export default LeadsPage;
