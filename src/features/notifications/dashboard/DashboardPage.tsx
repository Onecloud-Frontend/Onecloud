import React from 'react';
import PageShell from '@/shared/components/ui/PageShell';

/** Route: /notifications/dashboard */
const NotificationsDashboardPage: React.FC = () => (
  <PageShell domain="Notifications" title="Notification Center" description="Platform notification management — email, SMS, in-app, and push notification channels." />
);
export default NotificationsDashboardPage;
