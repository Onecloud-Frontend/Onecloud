import React from 'react';
import PageShell from '@/shared/components/ui/PageShell';

/** Route: /calendar/dashboard */
const CalendarDashboardPage: React.FC = () => (
  <PageShell domain="Calendar & Scheduling" title="Calendar" description="Organization-wide calendar, meetings, resource bookings, and scheduling." />
);
export default CalendarDashboardPage;
