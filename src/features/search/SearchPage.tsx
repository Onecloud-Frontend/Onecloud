import React from 'react';
import PageShell from '@/shared/components/ui/PageShell';

/** Route: /search */
const SearchPage: React.FC = () => (
  <PageShell domain="Enterprise Search" title="Global Search" description="Enterprise-wide semantic search across documents, employees, transactions, and records." />
);
export default SearchPage;
