import React from 'react';
import PageShell from '@/shared/components/ui/PageShell';

/** Route: /workflow/designer */
const WorkflowDesignerPage: React.FC = () => (
  <PageShell domain="Workflow" title="Process Designer" description="Visual drag-and-drop workflow designer for business process automation." />
);
export default WorkflowDesignerPage;
